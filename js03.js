// # 문제3 : 변수의 타입
// 다음 출력 값으로 올바른 것은?
var arr = [100, 200, 300];
console.log(typeof arr);
// 콘솔창에서는 object가 출력된다.
// array라는 타입이 있지만 object,array는 object로 출력된다.
// type이 array인지 아닌지 알고 싶다면 Array.isarray()를 쓰자.
